apiVersion: v1
kind: Service
metadata:
  name: learnflow-frontend
  namespace: learnflow
  labels:
    app: learnflow-frontend
spec:
  selector:
    app: learnflow-frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      nodePort: 30300
  type: NodePort
